# Definitions

- Indecomposable: doesn't decompose as $A oplus B$.
- Irreducible: simple, i.e. no nontrivial proper submodules. Implies irreducible.

# List of Notation

- $M(\lambda)$: Verma Modules

- $L(\lambda)$: Unique simple quotients of Verma modules.
